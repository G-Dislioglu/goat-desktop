# GOAT Desktop Recovery Plan

Date: 2026-05-29

This recovery plan resets the repo away from circular wording, redaction, and contract-hardening loops.

## What Changed

- `RADAR.md` is the tactical next-work file.
- `.specify/.app-goal.md` is the current product boundary.
- `STATE.md` and `SESSION-LOG.md` remain append-only history.
- Future implementation runs need visible product proof unless they name a concrete bug, failing test, or recovery contract.
- The next live proof is `GOAT-LIVE-001`: Builder cue reaches Desktop and produces a visible popup proposal with no effects before approval.

## Do Next

1. Run `GOAT-LIVE-001` with a controlled cue path.
2. Make missing Builder websocket configuration visible to the user if it prevents the proof.
3. Add a normal startup/package path after the live proof passes.
4. Choose one next capability only after the product path is proven.
